services:
  - type: web
    name: medikiosk-kiosk
    runtime: node
    rootDir: apps/kiosk-frontend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /